#include<iostream>
#include<stdlib.h>
#include<stdint.h>

using namespace std;

struct Node{
  int value;
  Node* xptr;
};

struct Node* xor(Node *x, Node *y){
  return (Nosde *)((uintptr_t) a ^ (uintptr_t) b);
}

void insert(element** head, int value){
  Node* temp = (Node *) malloc(sizeof(Node));
  temp->value = value;
  
  if (!*head)
    Node addr = xor(NULL, )
}

int main(){
  
  return 0;
}
